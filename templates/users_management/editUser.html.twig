{% extends 'base.html.twig' %}

{% block title %}&Eacute;diter un utilisateur
{% endblock %}

{% block body %}
	{% include 'nav.html.twig' %}
	<div class="single-row grid-center">
		<div class="">
			<div class="wrap-titles">
				<h2>&Eacute;diter
					{{user.firstName}}
					{{user.lastName}}</h2>
				<h2 class="back-to-dashboard">
					<a href="{{ path('users_management') }}">&#10229; Liste des elèves</a>
				</h2>
			</div>
			<div class="wrap">
				<div class="card">
					{{ form_start(userForm, {'attr': {'class': 'form'} }) }}
					{{ form_row(userForm.first_name) }}
					{{ form_row(userForm.last_name) }}
					{{ form_row(userForm.email) }}
					{{ form_row(userForm.birth_date, {'attr': {'class': 'select-date'} }) }}
					<button class="btn btn-primary-dk">&Eacute;diter</button>
					{{ form_end(userForm) }}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
